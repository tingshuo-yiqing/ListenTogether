# 2026-09-26 设备复测（517A776B 装机 + 云端真实曲库回归 + 过期重入闭环）

日期：2026-09-26 傍晚（17:0x–18:13）
设备：PHQ110（OPPO，Android 14），USB 联调；手机开个人热点供外部设备上网（**本轮约束：不得关闭热点、不得再开飞行模式**）
后端：云端旧版 release 20260924-0937（`http://8.166.126.136:3000`，**09-26 后端修复未部署**，本轮在旧后端上测试）
被测包：debug `517A776B05C30EEBF2E7A0E2B68FE315F959748F0C3C8057FC20B975B69D98A3`（09-26 补充小轮交付锚）

## 目的

统一真机测试 09-26 两轮修复的回归面（用户指示「使用真实曲库进行测试。测完了后上云再同步到github」）：

1. seek 确认单调时钟重构（`seekConfirmed` 纯函数）——行为应与旧版一致（无假「进度跳转未确认」横幅）；
2. 过期横幅「重新加入房间」经 `composeNickname` ——重入不再被昵称校验拒绝；
3. 顺带回归：云端真实曲库播放全链路、邀请二维码、`member-sim` 成员进出、服务端清扫行为。

## 装机与一致性

- `scripts/install-debug.ps1` 装机 + 启动；USB 曾两次掉线（传输中断）。
- `pm path` 回拉 base.apk：**首次回拉 SHA256 不匹配**——排查发现 `adb pull` 静默截断（4,587,520 / 23,049,926 字节，USB 抖动所致，`tail` 只看末行掩盖了报错）；USB 重连后重新回拉，SHA256 与锚**逐位一致**。
- 教训：装机一致性核对必须同时核对字节数与哈希；回拉大小不符即重拉，不进入后续测试。

## 真机场景与结果（全部通过）

| # | 场景 | 步骤与结果 | 证据 |
|---|---|---|---|
| 1 | 冷启 + `lastRoom` 预填 | 冷启动进首页，昵称/房间码按偏好预填，不静默入房 | 00-cold-start.png |
| 2 | 云端建房 + 真实曲库 | 地址 `http://8.166.126.136:3000` 建房，歌单 23 首（云端真实音乐） | 01-room.png、02-room-created.png |
| 3 | 播放全链路 | 选《有何不可》播放，`dumpsys media_session` PLAYING、位置按 1.0x 前进（两次采样间隔≈33s，位置推进 32,730ms ≈ 真实流逝） | — |
| 4 | seek 回归（修复①） | 拖回开头（20794→~6s 量级）、拖中间、快速连拖两次，**全部确认、无「进度跳转未确认」横幅**——单调时钟重构行为无回归 | 04-seek-to-start.png、05-seek-middle.png、06-quick-seeks.png |
| 5 | 展开页/上一首下一首/暂停恢复 | 展开页控制正常；下一首环形切歌；暂停→恢复位置连续 | 03-player-sheet.png、07-next-track.png |
| 6 | 邀请二维码反解 | 顶栏二维码截图经 zxing 独立解码（工具链见下），文本与 `InviteCode.encode` 一致：已剥 `:3000`、无令牌 | 08-invite-qr.png、08-invite-decoded.txt |
| 7 | 相机扫码入房（用户人工） | 另一台设备相机扫 PC 屏上显示的二维码 → 确认卡 → 入房成功，服务端播放状态保留 | 09-join-screen.png、10-scanned-join.png |
| 8 | `member-sim` 成员进出 | `node scripts/member-sim.mjs join <码> "脚本小王" --hold 20 --target http://8.166.126.136:3000`：成员上线（2 人、在线点亮）→ 到期离开（1 人、灰点），界面即时反映 | 11-member-online.png、12-member-left.png |
| 9 | 服务端清扫 → 房间回收（附带验证） | 房 72A3BBF1 成员会话过期后退出；房主随后被清扫、空房被回收；再以旧码加入 → 表单内联「房间不存在或已过期」，表单内容保留 | 17-join-24char.png |
| 10 | 过期横幅（成员侧） | 成员会话断网 >60s 被清扫：先「连接断开，正在重试」（重连中）→ 重连拿到 401 → 「房间或成员已失效，请退出后重新加入」横幅 | 13-expired-banner.png、14-after-retry.png |
| 11 | 24 字符昵称边界 | 昵称输入 35 字符 → 表单**输入即截断为 24**（`takeCodePoints(it, 24)`，MainActivity.kt:451；`input text` 35 字符后 uiautomator 实测框内恰 24）；以恰 24 字符昵称建房成功（服务端 1–24 校验边界通过），成员头像取首字「R」 | 16-long-nickname.png、19-room-created-24char.png |
| 12 | 过期横幅 + 重新加入（房主侧，修复②） | 房主会话断网 >60s 被清扫 → 过期横幅 → 点「重新加入房间」→ 换发新令牌、重新入房成功（已连接、在线点、24 字符昵称原样、歌单/当前曲状态恢复），**未触发 400/昵称拒绝** | 20/21/22 见下 |

第 12 项时序：70 秒断网窗口未触发清扫（清扫 tick 未落在窗口内，重连后仍「已连接」，20-host-expired.png）；加大到 95 秒后触发（21-host-expired-retry.png）；点「重新加入房间」约 2 分钟后重入成功（22-rejoined.png）。

> **断网手段说明（应约束修正）**：本轮前段用飞行模式制造 >60s 断网，会连带关闭手机热点（用户随即要求不得关闭热点/开启飞行模式）。飞行模式只是「让 App 在不退出情况下真实断网 60s+」的手段——按 HOME 保活（音频前台服务）、杀进程（冷启不静默入房、无横幅）都无法触发该产品行为（服务端本就对断网 60s+ 的成员清扫并作废令牌，真实用户进电梯即会遇到）。**后续复现此场景改用 `svc data disable`（仅断蜂窝数据，热点保持开启）**，本轮结尾已不再使用飞行模式。

## 结论

- 两处 09-26 修复在真机上无回归、修复②的链路（过期→重入）完整闭环。
- 24 码元昵称上限在 UI 层输入即截断，**无法从界面构造 >24 的请求**；`composeNickname` 重入包装是纵深防御（超长场景由单测覆盖，见 2026-09-26-legacy-fixes）。
- 旧后端清扫行为符合既有约定：60s 宽限、空房回收、回收后加入报「房间不存在或已过期」且表单内容保留（与夜轮场景④一致）。

## 未覆盖 / 如实标注

- 双人真机同屏（缺第二台手机；本轮二维码/重入含用户人工扫码一次，其余成员交互由 `member-sim` 覆盖）。
- 09-26 后端修复（清扫清空房主 + 首次上线立即接任）**未部署**，本轮跑在旧后端上——房主被清扫后房间无主、重入后不自动接任属旧版预期行为；部署验证另起（见部署记录）。
- 触感人工手感、2 倍系统字号、小屏布局、公网弱网注入、真实令牌作废：保持挂起（条件同前）。
- 飞行模式期间热点两次短暂中断（用户已指出）；已改为蜂窝数据断网方案，未再发生。

## 工具备注

- 二维码解码：`qr-scratch/DecodeQr.java` + zxing core-3.5.3（Maven Central），`javac -encoding UTF-8` 编译、解码输出写 UTF-8 文件规避 GBK 控制台乱码（临时工具，不入库）。
- UI 文本取证：`uiautomator dump` + 临时解析脚本（临时文件，不入库）。
- 截图经 `/sdcard` 中转（`exec-out screencap` 直传在本机输出损坏 PNG）。
