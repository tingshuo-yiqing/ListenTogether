node_modules/
dist/
.gradle/
build/
local.properties
.env
*.pem
*.key
*.jks
*.keystore
media/*
!media/catalog.json
!media/README.md
*.log
.idea/
.workbuddy/
deploy-artifacts/
demo-backend*.log
fault-proxy*.log
load15-*.log
# 个人音频不入库（demo-media 仅保留 catalog.json 引用的合成测试音）
demo-media/有何不可.mp3
# Kotlin 编译会话缓存
android/.kotlin/
# 例外：test-results 是验收证据目录，其中的 .log 属于归档证据必须入库
# （2026-09-23 实测：evidence/01-package-seg1.log 被上面的 *.log 静默排除，
#   只有逐个核对 git status 才发现，交付前务必确认证据文件真的进了提交）
!docs/test-results/**/*.log

# Gradle 门禁退出码采集（陷阱 1.7，本地工件不入库）
android/*.exit.txt

# 装机核对用的 APK 副本不入库（可由源码重建，哈希锚在 README）
docs/test-results/**/installed-*.apk
